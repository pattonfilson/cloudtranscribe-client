# CloudTranscribe Client

```
use CloudSystems\CloudTranscribe\CloudTranscribe;

$client = new CloudTranscribe([
	'api_key' => '',
	'api_secret' => '',
	'uri' => 'https://cloudtranscribe.example.com/api/',
]);
```
